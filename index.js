// imports
const express = require('express')

// create app
const app = express()

// define port
const PORT = 8001

// assign port to app
app.listen(PORT, () => console.log(`app started on port: ${PORT}`);
 )